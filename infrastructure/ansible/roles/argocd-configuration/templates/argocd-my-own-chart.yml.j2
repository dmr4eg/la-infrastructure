apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ own_chart_name }}-application"
  namespace: {{ argocd_namespace }}
spec:
  project: default
  destination:
    namespace: "{{ own_chart_name }}"
    server: "https://kubernetes.default.svc"

  sources:
    - repoURL: "{{ repo_url }}"
      targetRevision: main
      path: "{{ own_chart_path }}"
      helm:
        releaseName: "{{ own_chart_name }}"
        valueFiles:
          - "values.yaml"

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
