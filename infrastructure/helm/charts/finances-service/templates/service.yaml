apiVersion: v1
kind: Service
metadata:
  name: {{ include "finances-service.fullname" . }}
  labels:
    {{- include "finances-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finances-service.selectorLabels" . | nindent 4 }}
