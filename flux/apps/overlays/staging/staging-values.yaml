wordpressUsername: staginguser
wordpressPassword: stagingpassword
mariadb.auth.rootPassword: stagingrootpassword
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  ingressClassName: nginx
  hostname: staging.localhost
  annotations:
    # kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
