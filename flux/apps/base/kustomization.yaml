apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

labels:
  - pairs:
      app: myapp

resources:
  - postgresql.yaml
  - services
  - apigw.yaml

configMapGenerator:
  - name: common-config
    env: config.properties
# secretGenerator:
#   - name: common-secret
#     literals:
#       - password=secret
#       - username=admin
