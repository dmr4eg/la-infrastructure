apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: report-service
  namespace: report-service
spec:
  releaseName: report-service
  chart:
    spec:
      chart: report-service
      sourceRef:
        kind: HelmRepository
        name: report-service
  interval: 50m
  install:
    remediation:
      retries: 3
  values:
    ingress:
      enabled: true
      className: nginx
